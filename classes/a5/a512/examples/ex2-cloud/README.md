# Cloud example

This code can be deployed to most cloud providers, by configuring the cloud
to use this directory as the application directory, `npm install` as the
build command and `npm start` as the start command

